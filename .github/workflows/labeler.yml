# https://github.com/actions/labeler

name: 'PR Labeler 🏷️'
description: 'Add automatic labels to PRs based on the content of the PR'

on:
  - pull_request_target

jobs:
  labeler:
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/labeler@v5
